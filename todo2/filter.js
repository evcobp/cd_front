class ListNode {
    constructor () {
        this.head = null;
    }
    filter() {
        var newNode = new Node(value);
        newNode.next = this.head;
        this.head = newNode;

        var lowVal = x;
        var highVal = y;
        currentNode = this.head;
        if (currentNode.data >= x || currentNode.data <= y) {
            var newList = currentNode.data.remove();            
                return newList;
        }
    }
}